# Asset Import Notes

The ChatGPT GitHub connector available in this session can create text files and commits, but cannot stream local PNG/WebP files from `/mnt/data` as normal binary repository files.

That is why this archive is repo-ready: it contains the exact final `assets/` structure that should be copied into the repository root.

For the first code prototype, load `assets/manifest.json`.
